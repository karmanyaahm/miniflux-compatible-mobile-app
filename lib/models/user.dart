class User {
  User() {
    //assumes having all info
  }

  User.login() {
    //constructor with mandatory username password and server url

// create miniflux class instance and get fullUser
  }
}
